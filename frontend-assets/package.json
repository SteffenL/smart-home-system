{
  "name": "@langnes/smart-home-system-frontend-assets",
  "version": "1.0.0",
  "description": "Smart Home System Frontend Assets",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run scripts:build && npm run scss:build",
    "scripts:dev": "webpack --watch",
    "scripts:build": "webpack",
    "scss:build": "sass --style=compressed --no-source-map --no-error-css --stop-on-error style/main.scss dist/style/main.css",
    "scss:dev": "npm run scss:build -- --watch"
  },
  "author": "Steffen André Langnes",
  "license": "UNLICENSED",
  "dependencies": {
  },
  "devDependencies": {
    "@langnes/smart-home-system-shared": "file:../shared",
    "bulma": "^0.9.3",
    "copy-webpack-plugin": "^10.2.0",
    "copyfiles": "^2.4.1",
    "sass": "^1.48.0",
    "ts-loader": "^9.2.6",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4",
    "webpack": "^5.66.0",
    "webpack-cli": "^4.9.1"
  }
}
